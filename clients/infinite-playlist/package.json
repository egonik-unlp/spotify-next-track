{
  "name": "infinite-playlist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build worker-core --target web --release --out-dir pkg && npm run copy:wasm",
    "build": "npm run build:wasm",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "npm run build:wasm && wrangler dev",
    "deploy": "npm run build:wasm && wrangler deploy",
    "copy:wasm": "mkdir -p public/wasm && cp worker-core/pkg/worker_core.js worker-core/pkg/worker_core_bg.wasm public/wasm/"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.120.0"
  }
}
